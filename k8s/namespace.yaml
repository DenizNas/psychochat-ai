apiVersion: v1
kind: Namespace
metadata:
  name: psychochat
  labels:
    app.kubernetes.io/name: psychochat
    app.kubernetes.io/part-of: psychochat-platform
    # Required for NetworkPolicy namespaceSelector matching
    kubernetes.io/metadata.name: psychochat
